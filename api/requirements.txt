# Пусто, и это намеренно.
#
# Функция бота (telegram.py) написана на одной стандартной библиотеке.
# Файл нужен не ради содержимого, а ради его отсутствия: без него сборщик
# Vercel идёт искать зависимости выше по дереву и находит pyproject.toml
# проекта — а там geopandas, dask и torch. Установка такого набора в
# бессерверную функцию либо не помещается в лимит, либо занимает минуты
# на каждом деплое, и всё это ради модуля, которому не нужно ничего.
