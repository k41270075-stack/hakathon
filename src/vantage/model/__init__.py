"""Модель классификации пар «до / после».

Разделение на три модуля соответствует трём разным вопросам:

    siamese.py — что за архитектура и почему именно она;
    train.py   — как она обучается и как измеряется качество;
    infer.py   — как применяется к новым кандидатам.

PyTorch импортируется лениво внутри модулей: базовый гео-пайплайн
(``vantage scenes``, ``vantage money``) должен работать без установленного
torch, иначе на чужой машине перед защитой всё встанет из-за двух гигабайт
зависимостей, которые для этой команды не нужны.
"""

from __future__ import annotations

__all__ = ["infer", "siamese", "train"]
